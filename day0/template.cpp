#include <bits/stdc++.h>

#include "../shared.hpp"

int main(void) {
    std::vector<std::string> data = readFile("input.txt");

    long long sum = 0;

    std::cout << sum << std::endl;

    return 0;
}

// Solution: 